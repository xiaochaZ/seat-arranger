# 班级排座位软件 — 需求规格文档（PRD）

> 版本：v0.1（需求剖析版）｜日期：2026-08-28｜状态：已确认核心方向，待开发

## 1. 项目概述

一款给班级排座位的 Web 工具：输入班级名单，配置教室布局与排座规则（同桌、隔离、区域等，每条规则带权重），由算法自动生成"尽量满足所有规则"的随机座位表，并提供可视化网格展示、拖拽微调与导出。

- **产品定位**：老师/班主任使用的轻量工具，浏览器打开即用，无需安装。
- **核心价值**：把"排座位既要随机又要讲人情世故"这件麻烦事自动化。

## 2. 目标用户与使用场景

- 用户：中小学班主任、辅导员。
- 场景：每学期/每阶段换座位时，输入名单 → 设规则 → 一键生成 → 打印/发群。
- 班级规模：30~60 人（典型 40~50 人）。

## 3. 功能需求

### 3.1 名单管理
- **F1.1** 手动逐个添加学生（姓名必填）。
- **F1.2** 整段粘贴导入：每行一个姓名，自动解析。
- **F1.3** Excel/CSV 文件导入：支持 .xlsx / .xls / .csv，自动读取"姓名"列，可选读学号/性别/身高/备注列（字段映射）。
- **F1.4** 学生属性字段：姓名（必填）、学号、性别、身高、备注；字段用于后续高级规则（近视坐前排等）。
- **F1.5** 名单增删改、清空、去重校验。
- **F1.6** 多班级管理：保存多套班级名单，可切换、可重命名、可删除。

### 3.2 教室布局配置
- **F2.1** 行列数配置：如 6 排 × 8 列，界面可调。
- **F2.2** 双人桌模式：相邻两列组成一张课桌（同桌 = 同桌的左右两人）；支持"每排走道分隔"（走道在中间，左右各若干列）。
- **F2.3** 区域划分：用户可自定义区域（命名 + 框选若干座位），如"前排 1-2 排""靠窗列""中间区"。
- **F2.4** 特殊座位：标记"不可用"座位（坏桌椅），排座时跳过。
- **F2.5** 布局可保存为模板，随班级配置一并保存。

### 3.3 规则系统（核心）
每条规则 = 约束内容 + 权重。MVP 实现三类基础规则：

| 规则 | 说明 | 参数 |
|---|---|---|
| **同桌规则** | 指定两人必须同桌 | 学生A、学生B、权重 |
| **隔离规则** | 指定两人必须远离 | 学生A、学生B、远离程度（不同桌 / 至少隔 N 排 / 至少隔 N 列）、权重 |
| **区域规则** | 某学生（或按属性筛选的一组学生）必须坐进某区域 | 学生或筛选条件、目标区域、权重 |
| **禁坐规则**（顺带实现） | 某学生不得坐某座位/区域 | 学生、排除项、权重 |

**规则扩展性**（V2 再考虑）：性别分开/混坐、按身高降序、成绩帮扶配对、班干部固定位置、纪律差者隔离、固定座位不参与排。

### 3.4 权重机制
- **F4.1** 每条规则一个权重滑块（0~100），默认 50，直观展示。
- **F4.2** 硬约束开关：勾选后该规则必须满足（视为无穷权重）；无法满足时算法给出"无解"提示，指出冲突规则。
- **F4.3** 代价模型：`方案总代价 = Σ(规则权重 × 该规则违规程度)`。违规程度按距离量化（如同桌要求未满足记 1 倍、隔离要求实际相距 0 排记 N 倍）。
- **F4.4** 权重冲突自动权衡：算法搜索使总代价最小的方案。

### 3.5 排座算法
- **F5.1** 采用**模拟退火**（Simulated Annealing）+ 多次随机重启：随机初始布局 → 迭代交换座位降温 → 取最优。
- **F5.2** 每次生成多套方案（默认 10 套），按总代价从低到高展示，供挑选。
- **F5.3** 性能要求：50 人规模，单套方案生成 ≤ 2 秒，10 套 ≤ 20 秒（可设"快速/精细"档位）。
- **F5.4** 每次生成带随机性：同样条件两次生成结果不同。
- **F5.5** 固定座位不参与交换。

### 3.6 可视化 UI（页面结构）
- **F6.1 座位表网格视图**：按教室布局渲染网格，座位显示姓名；讲台方向标注在顶部。
- **F6.2 规则可视化**：
  - 同桌/隔离关系：用连线或同色边框标示；
  - 区域：不同区域底色区分；
  - 违规高亮：不满足规则的座位红色描边 + 角标提示原因。
- **F6.3 拖拽微调**：鼠标拖拽交换两人座位；交换后立即重新计算代价并提示"本次调整新增 X 条违规 / 消除 Y 条违规"。
- **F6.4 违规报告面板**：列出所有未满足规则（涉及谁、规则内容、当前实际、代价），可点击定位到对应座位。
- **F6.5 多方案切换**：左右切换查看各方案，选中后进入微调。
- **F6.6 规则管理界面**：添加/编辑/删除规则，列表展示，权重滑块实时可调，调整后一键重新生成。

### 3.7 导出与保存
- **F7.1** 导出座位表为图片（PNG，含标题、班级名、日期，可直接打印/发群）。
- **F7.2** 导出 Excel 座位表（.xlsx：行列坐标 + 姓名 + 同桌信息）。
- **F7.3** 整套数据本地持久化（localStorage）：班级、布局、规则、当前选中的座位表，下次打开自动恢复。
- **F7.4** 配置导出/导入为 JSON 文件，方便换电脑迁移与备份。

## 4. 非功能需求
- **N1 免安装**：浏览器打开即用，无构建、无后端。
- **N2 离线可用**：不依赖网络（Excel 解析库本地内置）。
- **N3 数据安全**：数据全部存本地，不上传；提供导出备份能力。
- **N4 兼容性**：Chrome/Edge 最新版即可，桌面优先。
- **N5 易用性**：全中文界面，新手 5 分钟上手；关键操作带提示。

## 5. 技术方案（建议）
- **形态**：纯前端单页 Web 应用，**零构建、零 npm 依赖**（规避本机 npm 源不稳定问题）。
- **实现**：单 HTML 文件（或 index.html + app.js + style.css 少量文件），原生 JS / 或轻量框架 CDN 本地化。
- **Excel 读写**：SheetJS（xlsx.full.min.js）下载到本地 lib 目录，离线可用。
- **数据存储**：localStorage + JSON 配置导出。
- **算法**：手写模拟退火（约 100 行内），代价函数可插拔，便于未来加新规则类型。
- **拖拽**：原生 HTML5 Drag & Drop 或 Pointer Events，无需额外库。

## 6. 迭代规划
**MVP（第一版）**
1. 名单：手动 + 粘贴 + Excel/CSV 导入
2. 布局：行列数 + 双人桌 + 走道 + 区域框选 + 不可用座位
3. 规则：同桌 / 隔离 / 区域 / 禁坐 + 权重滑块 + 硬约束
4. 算法：模拟退火 + 10 套方案
5. UI：网格视图 + 规则可视化 + 拖拽微调 + 违规面板
6. 导出：PNG 图片 + Excel

**V2（后续）**
- 高级规则：性别、身高、帮扶、固定座位、班干部位
- 多班级管理增强、布局模板库
- 打印样式优化（A4 精确排版）

## 7. 已确认补充（2026-08-29）
1. 讲台默认在顶部（第 1 排靠讲台），暂不支持左右朝向。
2. 座位卡片显示位置号（第 N 排第 M 桌）。
3. 支持保存多套历史方案（命名保存、切换、删除）。
