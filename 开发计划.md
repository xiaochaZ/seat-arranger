# 班级排座位软件 — 开发计划

> 版本：v1.0 ｜ 日期：2026-08-29 ｜ 配套文档：《需求文档.md》

## 1. 项目概览

- **目标**：纯前端、零构建、零 npm 依赖的 Web 排座工具，双击 `index.html` 即用，完全离线。
- **核心流程**：名单 → 布局 → 规则（含权重）→ 模拟退火生成多方案 → 可视化微调 → 导出。
- **技术基线**：原生 HTML/CSS/JS（ES2020+）、localStorage 持久化、SheetJS 本地化（Excel）、Canvas（PNG 导出）、手写模拟退火。

## 2. 技术架构

```
┌─────────────────────────────────────────────┐
│  UI 层  app.js（页面交互/状态/向导流程）      │
├──────────┬──────────┬──────────┬────────────┤
│ 数据模块 │ 算法模块  │ 渲染模块 │ 工具模块    │
│ storage  │ cost     │ render   │ export     │
│ roster   │ anneal   │ drag     │ parser     │
│ layout   │          │          │            │
│ rules    │          │          │            │
├─────────────────────────────────────────────┤
│ 存储: localStorage (key: seatplanner.v1)    │
│ 库: lib/xlsx.full.min.js (Excel 读写)        │
└─────────────────────────────────────────────┘
```

模块间单向依赖：`app → (roster/layout/rules) → cost → anneal → render/drag → export`。各模块无状态纯函数优先，便于单测与复用。

## 3. 文件结构

```
座位表/
├── 需求文档.md
├── 开发计划.md
├── index.html              # 单页入口，引入全部模块
├── css/
│   └── style.css           # 全部样式（网格/卡片/面板/对话框）
├── lib/
│   └── xlsx.full.min.js    # SheetJS（下载后本地化，离线可用）
└── js/
    ├── app.js              # 主控：状态、页面路由（步骤页）、事件绑定、初始化
    ├── storage.js          # localStorage 读写、JSON 配置导入/导出、默认数据
    ├── roster.js           # 名单：增删改查、多班级、粘贴解析
    ├── parser.js           # 文件解析：CSV（含 BOM 处理）、Excel 字段映射
    ├── layout.js           # 布局：行列/双人桌/走道/区域/不可用座位/座位号生成
    ├── rules.js            # 规则数据模型与 CRUD
    ├── cost.js             # 代价函数：单规则违规量 → 方案总代价、违规明细
    ├── anneal.js           # 模拟退火 + 多重启 + 生成 N 套方案
    ├── render.js           # 网格渲染、区域底色、同桌标示、违规高亮、多方案切换
    ├── drag.js             # 拖拽交换 + 即时重算代价提示
    └── export.js           # PNG 导出（Canvas 绘制）、Excel 座位表导出
```

## 4. 数据模型（预定义，各模块按此实现）

```js
// 班级
{ id: 'c1', name: '高一3班', students: [Student], createdAt: 1234567 }

// 学生
{ id: 's1', name: '张三', studentNo: '', gender: '', height: '', note: '' }

// 布局
{ rows: 6, cols: 8,               // 6 排 × 8 列
  aisleAfterCol: 4,               // 第 4 列后是走道（走道分隔左右，双人桌不跨走道）
  unavailable: ['r3c6'],          // 不可用座位坐标
  areas: [ { id: 'a1', name: '前排', seats: ['r1c1', 'r1c2'] } ] }

// 座位坐标格式：'r{排}c{列}'，从 1 开始；双人桌 = 同一排、相邻两列、不跨走道

// 规则
{ id: 'r1',
  type: 'pair' | 'separate' | 'area' | 'ban',
  aId: 's1', bId: 's2',           // pair/separate 用
  minRows: 1, minCols: 0,         // separate 用：至少隔几排/几列（0 表示不限）
  areaId: 'a1',                   // area/ban 用（ban 也可指具体座位坐标）
  seat: 'r1c1',                   // ban 指定具体座位时用
  weight: 80,                     // 0~100
  hard: false }                   // 硬约束开关

// 方案（座位表结果）
{ id: 'p1', name: '方案一', createdAt: 1234567,
  assignment: { 'r1c1': 's1', 'r1c2': 's5', ... },   // 座位 → 学生
  totalCost: 120 }

// 应用状态（整体持久化）
{ version: 1, classes: [Class], currentClassId: 'c1',
  layout: Layout, rules: [Rule],
  solutions: [Solution], currentSolutionId: 'p1' }
```

## 5. 开发阶段计划

### 阶段 0：项目骨架 ⏱ 0.5 天
**任务**
- [ ] 建立文件结构，index.html 引入所有 JS 模块与样式
- [ ] 顶部四步导航（①名单 ②布局 ③规则 ④排座结果），步骤间数据互通
- [ ] AppState 全局状态 + storage.js 骨架（默认数据、读写接口）
- [ ] 基础 CSS：布局框架、按钮、表单、对话框通用样式

**验收**：打开页面出现四步向导，可空步骤切换，刷新不报错。

---

### 阶段 1：名单管理 ⏱ 1 天
**任务**
- [ ] 手动添加/批量添加、删除、清空、按姓名搜索
- [ ] 粘贴导入（每行一个姓名，自动去重，提示重复项）
- [ ] CSV 导入：处理 UTF-8 BOM（防中文乱码）、分隔符自动识别（逗号/制表符）
- [ ] Excel 导入（SheetJS）：识别"姓名"列，可选学号/性别/身高列映射
- [ ] 多班级：班级列表、新建/重命名/切换/删除
- [ ] 名单行内编辑（姓名/学号/性别/身高/备注）

**验收**：导入 50 人 Excel 名单 1 秒内完成；中文不乱码；重复姓名有提示；切班数据互不干扰。

---

### 阶段 2：布局配置 ⏱ 1 天
**任务**
- [ ] 行列数设置（数字输入 + 快捷预设 6×8 / 5×8 / 4×7）
- [ ] 网格编辑器：点击座位切换状态（正常/不可用）
- [ ] 双人桌模式：自动按相邻列分组，走道列位置可调（`aisleAfterCol`），同桌不跨走道
- [ ] 区域工具：拖拽框选或点击多选座位 → 命名建区（默认快捷区：前排/后排/靠窗/中间）
- [ ] 讲台固定渲染在顶部（第 1 排上方）
- [ ] 座位号生成规则：`第{排}排第{桌}桌`（双人桌共用一个桌号）

**验收**：调整行列即时重绘网格；区域框选正确；不可用座位在后续排座中被跳过；座位号正确显示。

---

### 阶段 3：规则系统 ⏱ 1 天
**任务**
- [ ] 规则列表页：按类型分组展示，支持增/删/改
- [ ] 四种规则编辑器：
  - 同桌（pair）：选学生A、学生B
  - 隔离（separate）：选 A、B + 最小排距/列距
  - 区域（area）：选学生（或按属性筛选：如"性别=女"）+ 目标区域
  - 禁坐（ban）：选学生 + 指定区域或具体座位
- [ ] 权重滑块（0~100，拖动实时显示）+ 硬约束开关（勾选=必须满足）
- [ ] 规则命名（可选，默认自动生成描述文本）
- [ ] 规则描述自动生成（如"张三 与 李四 必须同桌（权重 80）"）

**验收**：四类规则均可创建并正确显示描述；权重与硬约束可修改；规则随配置保存。

---

### 阶段 4：代价函数 + 排座算法 ⏱ 1.5 天
**任务**
- [ ] cost.js：单规则违规量化（见 §6），汇总总代价
- [ ] 违规明细输出：每条未满足规则 + 涉及学生 + 当前实际 + 扣分
- [ ] anneal.js：随机初始布局 → 迭代随机交换 → 退火降温 → 多轮重启
- [ ] 固定学生/不可用座位不参与交换（禁坐、区域等通过代价自然惩罚）
- [ ] 生成 10 套方案，按总代价升序排列，去重（相同布局只留一套）
- [ ] 生成进度 UI（遮罩 + 进度条，避免用户误以为卡死）
- [ ] 无解检测：硬约束无法同时满足时给出冲突规则清单提示

**验收**：50 人 + 20 条规则，10 套方案生成 ≤ 20 秒；同桌/隔离/区域/禁坐规则被有效满足；无解场景能报出冲突。

---

### 阶段 5：可视化 UI ⏱ 1.5 天
**任务**
- [ ] 网格渲染：讲台顶部、区域底色、座位卡片（座位号 + 姓名）、不可用座位灰显
- [ ] 规则可视化：同桌同色边框 + 连线、隔离规则灰色虚线圈出
- [ ] 违规高亮：不满足规则的学生座位红框 + 角标数字（违规条数）
- [ ] 多方案切换：左侧方案列表（含代价），点击切换，当前方案高亮
- [ ] 违规报告面板：违规明细列表，点击定位闪烁对应座位
- [ ] 拖拽交换：拖动学生卡片到另一座位交换；松手后即时重算代价并弹提示"新增 X 条违规 / 消除 Y 条违规"
- [ ] 方案命名/保存（存入 solutions）、加载历史方案

**验收**：可视化要素齐全；拖拽交换流畅且提示准确；多方案切换无数据错乱；刷新页面后状态完整恢复。

---

### 阶段 6：导出与备份 ⏱ 1 天
**任务**
- [ ] PNG 导出：Canvas 按网格重绘（标题/班级名/日期/区域底色），输出 2x 高清图
- [ ] Excel 导出：行列坐标 + 姓名 + 桌号 + 同桌关系
- [ ] 整套配置导出 JSON（备份）、导入 JSON（恢复）
- [ ] 打印样式优化（A4 一页一张座位表）

**验收**：导出的 PNG 可直接打印/发群；Excel 打开无乱码；导出 JSON 后删掉数据再导入可完整恢复。

---

### 阶段 7：收尾打磨 ⏱ 0.5 天
**任务**
- [ ] 空状态引导（未导入名单时的操作提示动画）
- [ ] 边界处理：人数 > 座位数、座位数 > 人数、名单为空、规则引用已删除学生
- [ ] Chrome/Edge 兼容检查、中文界面文案统一
- [ ] 全流程走查：新建班级 → 导入 → 布局 → 规则 → 生成 → 微调 → 导出 → 备份

**验收**：边界场景有友好提示不白屏；全流程无阻塞。

## 6. 算法与代价函数设计

**违规量化**（cost.js 实现）：
| 规则 | 满足条件 | 不满足时违规量 |
|---|---|---|
| 同桌 pair | A、B 在同一桌（同排、相邻列、不跨走道） | 1 |
| 隔离 separate | 行距 ≥ minRows 且 列距 ≥ minCols（0=不限） | max(1, minRows-行距, minCols-列距) |
| 区域 area | 学生座位 ∈ 目标区域 | 1 |
| 禁坐 ban | 学生座位 ∉ 禁坐集合 | 1 |

- 总代价 = Σ(权重 × 违规量)；硬约束违规 → 该方案直接判无效（记 BIG=1e9，不参与排名）。
- 同桌规则若两人实际同座位不可能，成本函数天然惩罚交换。

**模拟退火参数**（anneal.js）：
- 初始温度 100，终止 0.1，降温系数 0.995
- 每温度迭代 = 人数 × 10 次随机交换（只交换普通学生；不可用座位永远空）
- 单套方案 = 1 次完整退火（约 50 人 × 2000 次评估，毫秒级）
- 10 套方案 = 10 次独立运行（不同随机种子），按总代价升序，去重

**复杂度**：评估一次代价 O(规则数)；50 人 20 条规则下全套生成远低于 20 秒预算。若实测超标，优化方向：增量代价（只重算被交换两人相关规则）。

## 7. 测试策略

- 每个阶段结束时手测验收清单（见各阶段"验收"）。
- 算法模块（cost/anneal）用 Node 直接跑单测脚本 `test/algorithm.test.js`（零依赖，node 内置 assert），覆盖：
  - 同桌规则满足/违反
  - 隔离距离量化正确
  - 区域/禁坐判定
  - 硬约束无解检测
  - 退火结果：代价不高于随机初值、固定座位不移动
- 存储模块：导入/导出 JSON 往返一致。

## 8. 里程碑

| 里程碑 | 内容 | 完成后效果 |
|---|---|---|
| M1 | 阶段 0~2 | 数据准备层：名单 + 布局可配置 |
| M2 | 阶段 3~4 | 核心引擎：规则 + 算法跑通，能出方案 |
| M3 | 阶段 5~6 | 完整可用：可视化 + 导出 + 备份 |
| M4 | 阶段 7 | 打磨交付：可正式使用 |

预计总工时：**约 8 个工作日**（业余时间 2~3 周）。

## 9. 风险与应对

| 风险 | 应对 |
|---|---|
| npm 源不稳定导致装依赖失败 | 全程零 npm 依赖，已规避 |
| SheetJS 本地文件缺失（未下载成功） | 单独下载 lib/xlsx.full.min.js（约 900KB）；CSV 路径不依赖它 |
| 模拟退火陷入局部最优 | 多重启 + 调大初始温度；必要时增量代价优化跑更多轮 |
| 机器重置导致数据丢失 | localStorage 之外提供 JSON 一键导出备份（界面常驻提醒） |
| Excel 中文乱码 | CSV 强制 UTF-8 BOM；xlsx 走 SheetJS 二进制解析无编码问题 |

## 10. 交付物清单

1. 完整可用的排座应用（index.html + 静态资源，双击即用）
2. 算法单测脚本 test/algorithm.test.js
3. 本开发计划与需求文档（持续更新）
